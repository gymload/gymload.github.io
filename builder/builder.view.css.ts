namespace $.$$ {
	const { rem } = $mol_style_unit

	$mol_style_define( $gymload_builder, {
		Settings: {
			flexDirection: 'column',
		},
		DayCount_labeler: {
			marginBottom: rem( 1 ),
		},
		Weeks: {
			marginTop: rem( 1 ),
		},
	} )
}